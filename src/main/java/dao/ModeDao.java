package dao;

import pojo.Mode;

public interface ModeDao {

    /**
     * save mode of user choice
     * @param mode
     * @return
     * @author Haoran Qi
     * @date
     */
    int saveMode(Mode mode);

}
